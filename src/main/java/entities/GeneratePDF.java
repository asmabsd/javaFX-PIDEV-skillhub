//package entities;
/*public class GeneratePDF {
    public static void main(String[] args) {
        String path = "invoice.pdf"; // Chemin du fichier PDF de sortie

        try {
            // Créez un nouveau document PDF
            Document document = new Document(PageSize.A4);

            // Créez un écrivain PDF pour écrire dans le document
            PdfWriter.getInstance(document, new FileOutputStream(path));

            // Ouvrez le document pour y écrire du contenu
            document.open();

            // Ajoutez du contenu au document
            document.add(new Paragraph("Ceci est un exemple de contenu ajouté au PDF."));

            // Fermez le document
            document.close();

            System.out.println("PDF généré avec succès à l'emplacement : " + path);
        } catch (FileNotFoundException | DocumentException e) {
            e.printStackTrace();
        }
    }
}*/